---
created: 2025-09-25
tags:
  - ps
  - boj
time: 27m56s
language:
  - python
topic:
  - dynamic programming
---
### Question
**문제**
계단 오르기 게임은 계단 아래 시작점부터 계단 꼭대기에 위치한 도착점까지 가는 게임이다. <그림 1>과 같이 각각의 계단에는 일정한 점수가 쓰여 있는데 계단을 밟으면 그 계단에 쓰여 있는 점수를 얻게 된다.

![300](https://u.acmicpc.net/7177ea45-aa8d-4724-b256-7b84832c9b97/Screen%20Shot%202021-06-23%20at%203.00.46%20PM.png)
<그림 1>

예를 들어 <그림 2>와 같이 시작점에서부터 첫 번째, 두 번째, 네 번째, 여섯 번째 계단을 밟아 도착점에 도달하면 총 점수는 10 + 20 + 25 + 20 = 75점이 된다.

![300](https://u.acmicpc.net/f00b6121-1c25-492e-9bc0-d96377c586b0/Screen%20Shot%202021-06-23%20at%203.01.39%20PM.png)
<그림 2>

계단 오르는 데는 다음과 같은 규칙이 있다.

1. 계단은 한 번에 한 계단씩 또는 두 계단씩 오를 수 있다. 즉, 한 계단을 밟으면서 이어서 다음 계단이나, 다음 다음 계단으로 오를 수 있다.
2. 연속된 세 개의 계단을 모두 밟아서는 안 된다. 단, 시작점은 계단에 포함되지 않는다.
3. 마지막 도착 계단은 반드시 밟아야 한다.

따라서 첫 번째 계단을 밟고 이어 두 번째 계단이나, 세 번째 계단으로 오를 수 있다. 하지만, 첫 번째 계단을 밟고 이어 네 번째 계단으로 올라가거나, 첫 번째, 두 번째, 세 번째 계단을 연속해서 모두 밟을 수는 없다.

각 계단에 쓰여 있는 점수가 주어질 때 이 게임에서 얻을 수 있는 총 점수의 최댓값을 구하는 프로그램을 작성하시오.

**입력**
입력의 첫째 줄에 계단의 개수가 주어진다.

둘째 줄부터 한 줄에 하나씩 제일 아래에 놓인 계단부터 순서대로 각 계단에 쓰여 있는 점수가 주어진다. 계단의 개수는 300이하의 자연수이고, 계단에 쓰여 있는 점수는 10,000이하의 자연수이다.

**출력**
첫째 줄에 계단 오르기 게임에서 얻을 수 있는 총 점수의 최댓값을 출력한다.

### Code
```python
# My Solution
def solution(scores):
    dp = [[0, 0]] * len(scores)
    dp[1] = [scores[1], scores[1]]

    for i in range(2, len(scores)):
        dp[i] = [
            max(dp[i-2][0], dp[i-1][1]) + scores[i],
            max(dp[i-2]) + scores[i]
        ]
    return max(dp[-1])

# Optimal Solution
def solution_optimal(scores):
    if len(scores) <= 3:
        return sum(scores)
    
    dp = [0] * len(scores)
    dp[1] = scores[1]
    dp[2] = dp[1]+scores[2]
    for i in range(3, len(scores)):
        dp[i] = max(dp[i-3] + scores[i-1], dp[i-2]) + scores[i]
    return dp[-1]    


if __name__ == "__main__":
    N = int(input())

    scores = [0]
    for _ in range(N):
        score = int(input())
        scores.append(score)

    print(solution(scores))
```

### Solution
#### My Solution
모든 경로를 고려하기 위해서 두 가지 배열을 이용하였다.
1. `dp[i][0]`: `i`번째 계단에서의 최고 계단 점수 → 이전 계단에 영향 안 받음
2. `dp[i][1]`: `i-1`번째 계단을 밟지 않았을 때, `i`번째 계단에서의 최고 계단 점수

각각에 대해서 다음과 같이 값을 구할 수 있다.
1. `dp[i][0] = max(dp[i-2][0], dp[i-1][1]) + scores[i]`
	1. `dp[i-2][0]`: `i-1`번째 계단을 밟지 않았을 때의 최고 점수
	2. `dp[i-1][1]`: `i-2`번째 계단을 밟지 않았을 때의 최고 점수
2. `dp[i][1] = map(dp[i-2][0], dp[i-2][1]) + scores[i]`
	- `i-1`번째 계단을 밟지 않았을 때의 최고 점수이므로, `i-2`번째 계단에서 바로 건너뛰었을 때를 가정

최종적으로 마지막 계단에서의 최고 점수는 `max(dp[-1][0], dp[-1][1])`로 구할 수 있다.

#### Another Solution
1. 계단이 2개 이하일 때: 전체 계단 점수의 총 합
2. 계단이 3개 이상일 때:
	- `dp[i]`는 `i`번째 인덱스에서의 최고 계단 점수
	- 현재 인덱스의 계단까지 도달하는 두 가지 경로를 생각할 수 있음
		1. `dp[i-3] + scores[i-1]`: `i-3, i-1`번째 계단을 밟은 경우
		2. `dp[i-2]`: `i-2`번째 계단을 밟고 건너뛴 경우
	- 최종적으로, `dp[-1]` 값이 마지막 계단에서의 최고 점수가 된다.

### References
- https://bio-info.tistory.com/158