---
created: 2025-08-12
tags:
  - ps
  - boj
time: 18m49s
language:
  - python
topic:
  - backtracking
---
### Question
**문제**
N개의 자연수와 자연수 M이 주어졌을 때, 아래 조건을 만족하는 길이가 M인 수열을 모두 구하는 프로그램을 작성하시오. N개의 자연수는 모두 다른 수이다.

- N개의 자연수 중에서 M개를 고른 수열

**입력**
첫째 줄에 N과 M이 주어진다. (1 ≤ M ≤ N ≤ 8)

둘째 줄에 N개의 수가 주어진다. 입력으로 주어지는 수는 10,000보다 작거나 같은 자연수이다.

**출력**
한 줄에 하나씩 문제의 조건을 만족하는 수열을 출력한다. 중복되는 수열을 여러 번 출력하면 안되며, 각 수열은 공백으로 구분해서 출력해야 한다.

수열은 사전 순으로 증가하는 순서로 출력해야 한다.

### My Answer
특정 갯수의 숫자 중에서 M개를 골라 조합하는 문제.
그래프 문제로 전환해서 해결하면 쉽게 해결할 수 있다.

각 숫자(1, 2, 3, …)를 그래프의 각 노드라고 생각하고, 해당 노드에서 주어진 길이 N만큼 탐색한다. 해당 길이만큼 탐색했다면, Backtracking을 이용해서 다시 이전 노드로 돌아와 탐색을 진행한다.

### Code
```python
import sys
N, M = list(map(int, sys.stdin.readline().split()))
num_list = list(map(int, sys.stdin.readline().split()))

def dfs(nums, visited, stack=[]):
    if len(stack) == M:
        return [stack[:]]
    
    num_seq = []
    for i, num in enumerate(nums):
        if visited[i]:
            continue

        visited[i] = True
        stack.append(num)
        num_seq += dfs(nums, visited, stack)[:]
        visited[i] = False
        stack.pop()

    return num_seq

visited = [False] * N
num_seqs = []
for i, num in enumerate(num_list):
    visited[i] = True
    cur_seqs = dfs(num_list, visited, [num])
    num_seqs.extend(cur_seqs)
    visited[i] = False

for num_seq in sorted(num_seqs):
    for num in num_seq:
        sys.stdout.write(str(num)+" ")
    sys.stdout.write("\n")
```

`dfs` 함수에서 "현재까지 탐색한 노드의 수"가 `M`과 일치하면 돌아가는 형식으로 코드를 구현하였다.
`num_list`에 있는 모든 숫자를 각각 시작점으로 놓고, 만들 수 있는 길이 `M`의 모든 수열을 구한 후, 숫자 크기 순으로 정렬해 출력하였다.

조금 더 개선된 풀이는 아래 출처를 참고바란다.
내 풀이는 정렬을 맨 나중에 진행했지만, `num_list`를 먼저 정렬한 후 백트래킹을 진행하면 훨씬 코드가 간결해진다.

### Reference
- https://aia1235.tistory.com/43