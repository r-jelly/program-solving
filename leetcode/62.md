---
created: 2025-07-28
tags:
  - ps
  - leetcode
time: 7m50s
language:
  - python
topic:
  - math
  - dynamic programming
  - combinatorics
---

### Question
There is a robot on an `m x n` grid. The robot is initially located at the **top-left corner** (i.e., `grid[0][0]`). The robot tries to move to the **bottom-right corner** (i.e., `grid[m - 1][n - 1]`). The robot can only move either down or right at any point in time.

Given the two integers `m` and `n`, return _the number of possible unique paths that the robot can take to reach the bottom-right corner_.

The test cases are generated so that the answer will be less than or equal to `2 * 109`.

**Example 1:**

![](https://assets.leetcode.com/uploads/2018/10/22/robot_maze.png)

**Input:** m = 3, n = 7
**Output:** 28

**Example 2:**

**Input:** m = 3, n = 2
**Output:** 3
**Explanation:** From the top-left corner, there are a total of 3 ways to reach the bottom-right corner:
1. Right -> Down -> Down
2. Down -> Down -> Right
3. Down -> Right -> Down

**Constraints:**
- `1 <= m, n <= 100`

### My Answer
왼쪽 위에서 오른쪽 아래까지 가는 경우의 수를 찾기. 로봇의 움직임이 `오른쪽, 아래` 이렇게 두 가지 경우만 허용되므로, 위나 왼쪽으로 돌아가는 경우는 고려하지 않아도 된다.
아마 수학 시간에 경우의 수 문제에서 한 번쯤 다뤄봤을 법한 문제이다.
두 가지 아이디어를 통해서 문제를 해결할 수 있었다.
1. 맨 윗줄은 오른쪽 이동으로만 구성될 수 있고, 맨 왼쪽줄은 아래쪽 이동으로만 구성될 수 있다.
   → 즉, 해당 줄의 각 칸에 도달하는 경우의 수는 1이다.
2. 다른 칸에 도달하는 경우의 수는, 바로 왼쪽 칸에서 오른쪽으로 움직이는 경우와 바로 위쪽 칸에서 아래쪽으로 움직이는 경우의 합으로 나타낼 수 있다.
   → 즉, $Map(i, j)=Map(i-1, j)+Map(i, j-1)$

### Code
```python
class Solution(object):
    def uniquePaths(self, m, n):
        """
        :type m: int
        :type n: int
        :rtype: int
        """
        grid = [[0]*n for _ in range(m)]
        for i in range(m):
            for j in range(n):
                if i==0 or j==0:
                    grid[i][j] = 1
                else:
                    grid[i][j] = grid[i-1][j] + grid[i][j-1]
					
        return grid[m-1][n-1]
```
- 최종적으로 O(mn)의 시간복잡도를 갖는 코드
> 수학적으로 해결해서 시간복잡도를 줄이는 방법은 없을까??
